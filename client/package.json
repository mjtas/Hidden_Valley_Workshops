{
  "name": "client",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "nodemon server.js",
    "build": "npm install --include=dev && npm install dotenv && npm install vite@5.2.11 && npx vite build",
    "preview": "vite preview",
    "test": "cross-env NODE_ENV=test jest --testTimeout=5000",
    "start": "node server.js"
  },
  "dependencies": {
    "ejs": "^3.1.9",
    "postmark": "^4.0.2",
    "vue": "^3.3.4",
    "vue-router": "^4.2.5"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^4.4.0",
    "cross-env": "^7.0.3",
    "jest": "^29.7.0",
    "supertest": "^7.0.0",
    "vite": "^5.2.11"
  }
}
